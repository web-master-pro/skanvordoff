mixin request(query, results)
    li.request&attributes(attributes)
        .request__query #{query} 
        .request__results #{results}

mixin requests()
    section.requests&attributes(attributes)
        .requests__block
            h2.requests__title
                span Популярные запросы
            ul.requests__list
                +request("спутник сатурна", "39 результатов")
                +request("болотная птица", "106 результатов")
                +request("серые клеточки", "4 результатов")
                +request("поделочный камень", "118 результатов")
                +request("рыба семейства вьюновых", "6 результатов")
                +request("буква и", "600 результатов")
                +request("хищная птица", "41 результатов")
            a(href="").requests__more-link &lt;&lt; Смотреть все &gt;&gt;

        .requests__block
            h2.requests__title
                span Последние запросы
            ul.requests__list
                +request("инструмент земли", "41 результатов")
                +request("(*****)холя)", "180 результатов")
                +request("серые клеточки", "4 результатов")
                +request("поделочный камень", "118 результатов")
                +request("рыба семейства вьюновых", "6 результатов")
                +request("буква и", "600 результатов")
                +request("хищная птица", "41 результатов")
            a(href="").requests__more-link &lt;&lt; Смотреть все &gt;&gt;
